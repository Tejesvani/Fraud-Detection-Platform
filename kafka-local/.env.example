# =============================================================================
# Fraud Detection Platform — Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values for your environment.
# NEVER commit .env to version control (it is already in .gitignore).
#
# All values shown below are the defaults used when a variable is absent.
# The project works out-of-the-box without a .env file.
#
# Docker Compose usage (from kafka-local/):
#   docker compose --env-file ../.env up -d
# =============================================================================

# Docker Infrastructure Variables
POSTGRES_USER=[fraud_user]
POSTGRES_PASSWORD=[fraud_password]
POSTGRES_DB=[fraud_detection_db]
POSTGRES_PORT=[5432]

# Kafka broker advertised host
KAFKA_ADVERTISED_HOST=[localhost]
KAFKA_PORT=[9092]