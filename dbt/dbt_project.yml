name: fraud_detection_analytics
version: '1.0.0'
config-version: 2

profile: fraud_detection

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  fraud_detection_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
    aggregates:
      +materialized: table
